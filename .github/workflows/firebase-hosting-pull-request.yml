# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
# and has additional modifications.

name: pr-preview
'on': pull_request
jobs:
  build-and-preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: subosito/flutter-action@v2
        with:
          channel: 'master'
          flutter-version: '3.17.0-5.0.pre.65'
      - run: flutter build web --profile --dart-define=Dart2jsOptimization=O0 -t example/basic-example/main.dart
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_KERNML }}'
          projectId: kernml
